rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Regra global de Autenticação para Maganta CRM
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
